import math
I = [0.1, 0.2, 0.3, 0.4]
Q = [0.5, 0.6, 0.7, 0.8]
iq_list = list(zip(I, Q))
print(iq_list[::-1])
# for idx, (i,q) in enumerate(iq_list):
#     magnitude =  math.sqrt(i**2 + q**2)
#     print(f"Magnitude:{idx} {magnitude:.2f}")

